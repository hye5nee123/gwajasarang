<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
  
<mapper namespace="com.yedam.orders.mapper.Ordersmapper">
  <select id="selectList" resultType="orders">
    SELECT * FROM orders ORDER BY 1
  </select>


    <insert id="insertOrders" parameterType="orders">
    INSERT INTO orders (order_code, member_code, order_date, order_address, order_name, order_phone, memo, delivery_fee, total_price)
    VALUES(orders_seq.nextval, #{member_code}, #{order_date}, #{order_address}, #{order_name}, #{order_phone}, #{memo}, #{delivery_fee}, #{total_price})
  </insert>

    
  </mapper>