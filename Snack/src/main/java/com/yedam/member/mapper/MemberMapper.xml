<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.yedam.member.mapper.MemberMapper"> 

  <select id="selectMember" resultType="member">
    select * from member where id=#{id} and pw=#{pw}
  </select>

  <select id="selectList" resultType="member">
	select * from member
  </select>
  
  <insert id="insertMember" resultType="member">
  	insert into member(id, pw, member_name, member_phone, member_adress, email)
  	values(#{id}, #{pw}, #{memeberName}, #{memberPhone}, #{memberAdress}, #{email})
  </insert>
  
</mapper>